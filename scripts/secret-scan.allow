# secret-scan.sh allowlist: fixed strings that are PROVABLY fake. One per line;
# comment/blank lines ignored. Every entry names its fixture — never add a
# real credential here (real secrets live in ignored .env files, never in git).
# Fake provider keys in backend AI tests (contain stage markers, no entropy):
sk-ant-test-anthropic-key-stage14-ccc3
sk-ant-test-anthropic-key-stage14-eee5
AIza-test-gemini-key-stage12-ddd4
# Fake rotation fixtures in SettingsScreen.test.tsx:
sk-test-key-9999
gsk-brand-new-7777
# Negative-path fixture in test_auth.py (literally not a token):
no-such-token
# Placeholder DSN passwords (example files only, never a live host):
changeme-local-only
postgres:postgres@localhost
u:p@
