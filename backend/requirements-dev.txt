# Development / test dependencies (pinned). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
# Stage 20 (roadmap-21): pytest 9.x fixes PYSEC-2026-1845 (/tmp sibling attack).
pytest==9.1.1
ruff==0.8.4
mypy==1.14.1
# Dependency-audit gate (docs/SECURITY_SPEC.md §10, scripts/verify.sh).
pip-audit==2.10.1
